import { roomEnum } from '../enmus/index';

const room_0_config = {
  link: {
    outPut: [],
    input: [],
  },
};

const roomPlan: Map<roomEnum, any> = new Map([[roomEnum.room_0, room_0_config]]);

export default roomPlan;
